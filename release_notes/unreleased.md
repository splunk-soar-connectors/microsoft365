**Unreleased**

* Embedded messages now replace primary ES finding evidence only for explicitly configured trusted reporter addresses.
* Untrusted senders retain the outer email title, headers, body, URLs, and payload attachments as primary finding evidence.
